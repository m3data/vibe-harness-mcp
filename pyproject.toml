[project]
name = "vibe-harness"
version = "0.1.0"
description = "MCP server that tunes human-AI interaction rhythm based on working modes and biosignal data"
requires-python = ">=3.10"
license = "ESL-A-0.1"
dependencies = [
    "mcp>=1.25.0",
]

[project.scripts]
vibe-harness = "server:main"
